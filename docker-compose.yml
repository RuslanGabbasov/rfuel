services:
  rfuel:
    image: ghcr.io/ruslangabbasov/rfuel:latest
    restart: unless-stopped
    network_mode: host
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - SOCKS_PROXY=${SOCKS_PROXY:-}
      - DATA_DIR=/data
    volumes:
      - rfuel-data:/data

volumes:
  rfuel-data:
